const initialState = [
    {id: 1, name: "Project 1"},
    {id: 2, name: "Project 2"},
    {id: 3, name: "Project 3"}
]

const projectReducer = (state = initialState, action) => {
    return state;
}

export default projectReducer;
